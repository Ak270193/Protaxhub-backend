# Server
PORT=4000

# Supabase (Project Settings -> API)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Twilio (for OTP login SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# SMTP (for onboarding + document notification emails — Gmail app password, SendGrid, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Pro Tax Hub <no-reply@protaxhub.com.au>"

# Where new-onboarding and client-upload notifications get sent
NOTIFY_EMAIL=smart.kukreja93@gmail.com
